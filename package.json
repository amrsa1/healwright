{
  "name": "healwright-test",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "SELF_HEAL=1 npx playwright test",
    "test:headed": "SELF_HEAL=1 npx playwright test --headed"
  },
  "dependencies": {
    "@playwright/test": "^1.58.2",
    "dotenv": "^17.3.1",
    "healwright": "*"
  }
}
